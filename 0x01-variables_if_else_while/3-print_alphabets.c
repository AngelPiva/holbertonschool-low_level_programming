#include <stdio.h>

/**
*main - abcABC
*Return: 0
*/

	int main(void)
	{
		int n, N;
	for (n = 'a'; n <= 'z'; n++)
	putchar (n);
	for (N = 'A'; N <= 'Z'; N++)
	putchar (N);
	putchar ('\n');
	return (0);
}
